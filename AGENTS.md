# Nano Backend.AI — Agent Entry Point

`CLAUDE.md` is the canonical agent guidance document for this repository.

Agents that read `AGENTS.md` first, including Codex-style tools, should follow
[`CLAUDE.md`](CLAUDE.md) for the shared repository rules:

- language, formatting, lint, and test conventions
- dependency and API response rules
- work decomposition into Epic → Story → Task
- available `.claude/skills`

Keep repo-wide policy in `CLAUDE.md`. Add tool-specific notes here only when an
agent cannot consume `CLAUDE.md` directly.
